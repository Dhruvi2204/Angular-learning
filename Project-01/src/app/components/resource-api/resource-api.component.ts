import { Component } from '@angular/core';

@Component({
  selector: 'app-resource-api',
  imports: [],
  templateUrl: './resource-api.component.html',
  styleUrl: './resource-api.component.css',
})
export class ResourceApiComponent {
  // ! in documentation: resource is experimental. It's ready for you to try, but it might change before it is stable.
}
