import { ShowPerformancePipe } from './show-performance.pipe';

describe('ShowPerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
