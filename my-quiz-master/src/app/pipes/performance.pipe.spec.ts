import { PerformancePipe } from './performance.pipe';

describe('PerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new PerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
